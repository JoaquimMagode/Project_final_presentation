-- Run this SQL script in your MySQL client to create the database
-- You can use MySQL Workbench, phpMyAdmin, or command line

CREATE DATABASE IF NOT EXISTS imap_solution_db;
USE imap_solution_db;

-- Verify database creation
SELECT 'Database imap_solution_db created successfully!' as message;